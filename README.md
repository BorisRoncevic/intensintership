After the basic crud, junit and swagger that you asked for I set out to make one of the things that was interesting to me.The most interesting thing in my code which is pagination.In my code I set it to return 5 users per page. I have always been interested in how it looks like to maintain a site and how much it costs to have a site and I recently started with AWS and things like pagination reduce the AWS bill.For pagination I used Spring Data Pageable on the backend which allows me to request a specific page and size from the database. Instead of returning all users the API returns only a set of data with information like total pages. On the frontend I keep track of the current page in state and send it to the backend on every request.
 so I would say that pagination is the most interesting part of my code.